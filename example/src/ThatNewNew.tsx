import React from 'react';
import { SafeAreaView } from 'react-native';
import { Text } from 'react-native-ui-lib';

export const ThatNewNew = () => {
  return (
    <SafeAreaView>
      <Text>That New New</Text>
    </SafeAreaView>
  );
};
